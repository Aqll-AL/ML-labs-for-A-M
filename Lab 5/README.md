### ARTI308 Lab 5

This lab focuses on predicting **Order_Status** using a **supervised machine learning classification** model.

### What I Did

- added a new feature: `driver_to_restaurant_distance`
- modified the `is_peak_hour` rule
- reduced `Item_Name` categories using `top_k`
- prepared the data for training
- trained a **Random Forest Classifier**
- checked feature importance
- applied **feature selection** with `SelectFromModel`