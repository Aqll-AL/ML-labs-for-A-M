# Lab 8 – K-Nearest Neighbors (KNN)
**ARTI308: Machine Learning**

---

## Overview

In this lab, you build a **K-Nearest Neighbors (KNN)** classification model on an artificial dataset. The goal is to classify records into one of two target classes, tune the value of K using the elbow method, and evaluate model performance using a confusion matrix and classification report.